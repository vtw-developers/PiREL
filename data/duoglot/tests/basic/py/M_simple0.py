bar = True
core = 1
pi = 3
def baz(x, y, z): pass
def good(x, y): 
    z = x + y
m = (core + core) * pi
if (m == 4):
    a = pi + core
    if (core < 0):
        fd = bar
    for i in range(10):
        core = core + i
        core = i * core
    c = a * a
b = m - pi