let _enum_list = [...((a, b) => Array.from(Array(Math.min(b.length, a.length)), async (_, i) => [a[i], b[i]]))(sdd, s)];
for (let i = 0; i < _enum_list.length; i++) {
    let v = _enum_list[i];
    a += i;
}
let _enum_list = [...((a, b) => Array.from(Array(Math.min(b.length, a.length)), async (_, i) => [a[i], b[i]]))(sdd, s)];
for (let i = 0; i < _enum_list.length; i++) {
    let [a, b] = _enum_list[i];
    a += i;
    c = 5;
}
for (let i = 0; i < d.length; i++) {
    let [a, b] = d[i];
    a += i;
    c = 5;
}
for (let i = 0; i < d.length; i++) {
    let f = d[i];
    a += i;
}