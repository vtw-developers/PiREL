let bar = true;
let core = 1;
let pi = 3;
function baz(x, y, z) {}
function good(x, y) {
  let z = x + y;
}
let m = (core + core) * pi;
if (m === 4) {
  let a = pi + core;
  if (core < 0) {
    let fd = bar;
  }
  for (let i = 0; i < 10; i++) {
    core = core + i;
    core = i * core;
  }
  let c = a * a;
}
let b = m - pi;