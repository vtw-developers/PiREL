```
midVal1 = nums1[i + k // 2 - 1] if i < len(nums1) else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if k < len(nums2) else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if i < k else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if len(nums1) < len(nums2) else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if k < 10 else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if i < j else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if len(nums1) > i else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if j < k else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if k < 100 else float('inf')
```
```
midVal1 = nums1[i + k // 2 - 1] if len(nums1) < 10 else float('inf')
```