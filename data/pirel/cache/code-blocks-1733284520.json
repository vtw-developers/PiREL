{
  "template": "[matrix[top][j] for j in range(__, right + 1)]",
  "lang": "py",
  "temperature": 0.7,
  "code_blocks": [
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(-1, right + 1)]",
    "[matrix[top][j] for j in range(10, right + 1)]",
    "[matrix[top][j] for j in range(100, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][j] for j in range(0, right)]",
    "[matrix[top][j] for j in range(0, right - 1)]",
    "[matrix[top][j] for j in range(0, right + 2)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(100, right + 1)]",
    "[matrix[top][j] for j in range(-5, right + 1)]",
    "[matrix[top][j] for j in range(left, right)]",
    "[matrix[top][j] for j in range(left, 100)]",
    "[matrix[top][j] for j in range(left, -5)]",
    "[matrix[top][j] for j in range(0, right)]",
    "[matrix[top][j] for j in range(1, right)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][j] for j in range(__, right)]",
    "[matrix[top][j] for j in range(__ + 1, right + 1)]",
    "[matrix[top][j] for j in range(left + 1, __)]",
    "[matrix[top][j] for j in range(left, __)]",
    "[matrix[top][j] for j in range(left, __ + 1)]",
    "[matrix[top][j] for j in range(left + 1, right)]",
    "[matrix[top][j] for j in range(left + 1, __ + 1)]",
    "[matrix[top][j] for j in range(__ + 1, right)]",
    "[matrix[top][j] for j in range(__ + 1, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(-1, right + 1)]",
    "[matrix[top][j] for j in range(-2, right + 1)]",
    "[matrix[top][j] for j in range(-3, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][j] for j in range(start, right + 1)]",
    "[matrix[top][j] for j in range(__, end + 1)]",
    "[matrix[top][j] for j in range(col, right + 1)]",
    "[matrix[top][j] for j in range(__, right)]",
    "[matrix[top][j] for j in range(l, right + 1)]",
    "[matrix[top][j] for j in range(x, right + 1)]",
    "[matrix[top][j] for j in range(__, r + 1)]",
    "[matrix[top][j] for j in range(start_col, right + 1)]",
    "[matrix[top][j] for j in range(__, __ + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][j] for j in range(right, right + 1)]",
    "[matrix[top][j] for j in range(__, left + 1)]",
    "[matrix[top][j] for j in range(__, left)]",
    "[matrix[top][j] for j in range(left, __ + 1)]",
    "[matrix[top][j] for j in range(left, __)]",
    "[matrix[top][j] for j in range(__, right)]",
    "[matrix[top][j] for j in range(right, __ + 1)]",
    "[matrix[top][j] for j in range(right, __)]",
    "[matrix[top][j] for j in range(__, right + 2)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]",
    "[matrix[top][j] for j in range(0, right + 1)]",
    "[matrix[top][j] for j in range(1, right + 1)]",
    "[matrix[top][j] for j in range(2, right + 1)]",
    "[matrix[top][j] for j in range(3, right + 1)]",
    "[matrix[top][j] for j in range(4, right + 1)]",
    "[matrix[top][j] for j in range(5, right + 1)]",
    "[matrix[top][j] for j in range(6, right + 1)]",
    "[matrix[top][j] for j in range(7, right + 1)]",
    "[matrix[top][j] for j in range(8, right + 1)]",
    "[matrix[top][j] for j in range(9, right + 1)]"
  ],
  "num_code_blocks": 300
}
