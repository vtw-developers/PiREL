{
  "template": "return __ * tot if INT_MIN <= __ * tot <= INT_MAX else INT_MAX",
  "lang": "py",
  "temperature": 0.7,
  "code_blocks": [
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return 10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "return -10 * tot if INT_MIN <= -10 * tot <= INT_MAX else INT_MAX",
    "return 0.5 * tot if INT_MIN <= 0.5 * tot <= INT_MAX else INT_MAX",
    "return -0.5 * tot if INT_MIN <= -0.5 * tot <= INT_MAX else INT_MAX",
    "return tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return product * tot if INT_MIN <= product * tot <= INT_MAX else INT_MAX",
    "return amount * tot if INT_MIN <= amount * tot <= INT_MAX else INT_MAX",
    "return value1 * tot if INT_MIN <= value1 * tot <= INT_MAX else INT_MAX",
    "return value2 * tot if INT_MIN <= value2 * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "return e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "return f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "return g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "return h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "return i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "return j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return 10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "return -10 * tot if INT_MIN <= -10 * tot <= INT_MAX else INT_MAX",
    "return tot + tot if INT_MIN <= tot + tot <= INT_MAX else INT_MAX",
    "return tot - tot if INT_MIN <= tot - tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return value1 * tot if INT_MIN <= value1 * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return multiplier * tot if INT_MIN <= multiplier * tot <= INT_MAX else INT_MAX",
    "return prod * tot if INT_MIN <= prod * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return 3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX",
    "return -3 * tot if INT_MIN <= -3 * tot <= INT_MAX else INT_MAX",
    "return 4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX",
    "return -4 * tot if INT_MIN <= -4 * tot <= INT_MAX else INT_MAX",
    "return 5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return res * tot if INT_MIN <= res * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return prod * tot if INT_MIN <= prod * tot <= INT_MAX else INT_MAX",
    "return amount * tot if INT_MIN <= amount * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return res * tot if INT_MIN <= res * tot <= INT_MAX else INT_MAX",
    "return prod * tot if INT_MIN <= prod * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return quantity * tot if INT_MIN <= quantity * tot <= INT_MAX else INT_MAX",
    "return count * tot if INT_MIN <= count * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "return tot * tot + 1 if INT_MIN <= tot * tot + 1 <= INT_MAX else INT_MAX",
    "return tot * tot - 1 if INT_MIN <= tot * tot - 1 <= INT_MAX else INT_MAX",
    "return tot // 2 if INT_MIN <= tot // 2 <= INT_MAX else INT_MAX",
    "return tot % 2 if INT_MIN <= tot % 2 <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return 3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX",
    "return -3 * tot if INT_MIN <= -3 * tot <= INT_MAX else INT_MAX",
    "return 4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX",
    "return -4 * tot if INT_MIN <= -4 * tot <= INT_MAX else INT_MAX",
    "return 5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "# Variant 1\nreturn x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nreturn y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX\n\n# Variant 3\nreturn z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX\n\n# Variant 4\nreturn a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX\n\n# Variant 5\nreturn b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX\n\n# Variant 6\nreturn c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX\n\n# Variant 7\nreturn m * tot if INT_MIN <= m * tot <= INT_MAX else INT_MAX\n\n# Variant 8\nreturn n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX\n\n# Variant 9\nreturn p * tot if INT_MIN <= p * tot <= INT_MAX else INT_MAX\n\n# Variant 10\nreturn q * tot if INT_MIN <= q * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return multiplier * tot if INT_MIN <= multiplier * tot <= INT_MAX else INT_MAX",
    "return product * tot if INT_MIN <= product * tot <= INT_MAX else INT_MAX",
    "return value1 * tot if INT_MIN <= value1 * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "return tot + tot if INT_MIN <= tot + tot <= INT_MAX else INT_MAX",
    "return tot - tot if INT_MIN <= tot - tot <= INT_MAX else INT_MAX",
    "return tot / tot if INT_MIN <= tot / tot <= INT_MAX else INT_MAX",
    "return tot % tot if INT_MIN <= tot % tot <= INT_MAX else INT_MAX",
    "return tot ** tot if INT_MIN <= tot ** tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "return z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return product * tot if INT_MIN <= product * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "return z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return product * tot if INT_MIN <= product * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "return e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "return f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "return g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "return h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "return i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "return j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "return z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return product * tot if INT_MIN <= product * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return var * tot if INT_MIN <= var * tot <= INT_MAX else INT_MAX",
    "return value1 * tot if INT_MIN <= value1 * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return val1 * tot if INT_MIN <= val1 * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return 10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "return -10 * tot if INT_MIN <= -10 * tot <= INT_MAX else INT_MAX",
    "return 100 * tot if INT_MIN <= 100 * tot <= INT_MAX else INT_MAX",
    "return -100 * tot if INT_MIN <= -100 * tot <= INT_MAX else INT_MAX",
    "return tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "# Variant 1\nreturn 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nreturn 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX\n\n# Variant 3\nreturn -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX\n\n# Variant 4\nreturn 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX\n\n# Variant 5\nreturn -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX\n\n# Variant 6\nreturn 3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX\n\n# Variant 7\nreturn -3 * tot if INT_MIN <= -3 * tot <= INT_MAX else INT_MAX\n\n# Variant 8\nreturn 4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX\n\n# Variant 9\nreturn -4 * tot if INT_MIN <= -4 * tot <= INT_MAX else INT_MAX\n\n# Variant 10\nreturn 5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return 10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "return -10 * tot if INT_MIN <= -10 * tot <= INT_MAX else INT_MAX",
    "return tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "return -tot * tot if INT_MIN <= -tot * tot <= INT_MAX else INT_MAX",
    "return tot * tot * tot if INT_MIN <= tot * tot * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -2 * tot if INT_MIN <= -2 * tot <= INT_MAX else INT_MAX",
    "return 3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX",
    "return -3 * tot if INT_MIN <= -3 * tot <= INT_MAX else INT_MAX",
    "return 4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX",
    "return -4 * tot if INT_MIN <= -4 * tot <= INT_MAX else INT_MAX",
    "return 5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "return z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return product * tot if INT_MIN <= product * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "return e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "return f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "return g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "return h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "return i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "return j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return prod * tot if INT_MIN <= prod * tot <= INT_MAX else INT_MAX",
    "return value1 * tot if INT_MIN <= value1 * tot <= INT_MAX else INT_MAX",
    "return val1 * tot if INT_MIN <= val1 * tot <= INT_MAX else INT_MAX",
    "return 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "return 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "return 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "return -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "return 10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "return -10 * tot if INT_MIN <= -10 * tot <= INT_MAX else INT_MAX",
    "return tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "return (tot + 1) * tot if INT_MIN <= (tot + 1) * tot <= INT_MAX else INT_MAX",
    "return (tot - 1) * tot if INT_MIN <= (tot - 1) * tot <= INT_MAX else INT_MAX",
    "return tot / tot if INT_MIN <= tot / tot <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return count * tot if INT_MIN <= count * tot <= INT_MAX else INT_MAX",
    "return res * tot if INT_MIN <= res * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return value * total if INT_MIN <= value * total <= INT_MAX else INT_MAX",
    "return x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "return n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "return val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX",
    "return result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "return num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX",
    "return value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX",
    "return res * tot if INT_MIN <= res * tot <= INT_MAX else INT_MAX",
    "return number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX",
    "return multiplier * tot if INT_MIN <= multiplier * tot <= INT_MAX else INT_MAX",
    "return prod * tot if INT_MIN <= prod * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "return e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "return f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "return g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "return h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "return i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "return j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "return a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "return b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "return c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "return d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "return e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "return f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "return g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "return h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "return i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "return j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX"
  ],
  "num_code_blocks": 282
}
