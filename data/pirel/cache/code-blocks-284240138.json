{
  "template": "sign * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
  "lang": "py",
  "temperature": 0.7,
  "code_blocks": [
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot else INT_MAX",
    "sign * tot if __ <= __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= __ <= __ else INT_MAX",
    "sign * tot if sign * tot <= __ else INT_MAX",
    "sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "sign * tot if __ <= __ else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= INT_MAX else INT_MAX",
    "sign * tot if INT_MIN <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= sign * tot else INT_MAX",
    "sign * tot if __ <= INT_MAX <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if __ <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 42 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -42 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 123456789 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= 0 else INT_MAX",
    "sign * tot if sign * tot != 0 else INT_MAX",
    "sign * tot if sign * tot > 0 else INT_MAX",
    "sign * tot if sign * tot < 0 else INT_MAX",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "sign * tot if -1e10 <= sign * tot <= 1e10 else 1e10",
    "sign * tot if -1000000000 <= sign * tot <= 1000000000 else 1000000000",
    "sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "sign * tot if -100 <= sign * tot <= 100 else 100",
    "sign * tot if -10 <= sign * tot <= 10 else 10",
    "sign * tot if -3 <= sign * tot <= 3 else 3",
    "sign * tot if -1 <= sign * tot <= 1 else 1",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else float('inf')",
    "sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else sys.maxsize",
    "sign * tot if -sys.maxsize - 1 <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -float('inf') - 1 <= sign * tot <= float('inf') else float('inf')",
    "sign * tot if -1e100 - 1 <= sign * tot <= 1e100 else 1e100",
    "sign * tot if -1.7976931348623157e+308 - 1 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -INF <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1000 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 10000 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100000 <= sign * tot <= INT_MAX else INT_MAX",
    "# Variant 1\nsign * tot if sign * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nsign * tot if __ <= sign * tot else INT_MAX\n\n# Variant 3\nsign * tot if __ <= sign * tot <= __ else INT_MAX\n\n# Variant 4\nsign * tot if __ <= sign * tot <= INT_MAX else __\n\n# Variant 5\nsign * tot if __ <= sign * tot else __\n\n# Variant 6\nsign * tot if sign * tot <= __ else INT_MAX\n\n# Variant 7\nsign * tot if __ <= sign * tot <= __ else __\n\n# Variant 8\nsign * tot if sign * tot <= INT_MAX else __\n\n# Variant 9\nsign * tot if __ <= __ else INT_MAX\n\n# Variant 10\nsign * tot if __ <= __ <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= -INT_MAX and sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= INT_MIN and sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= 0 and sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= -INT_MAX and sign * tot <= 0 else INT_MAX",
    "sign * tot if sign * tot >= INT_MIN and sign * tot <= 0 else INT_MAX",
    "sign * tot if sign * tot >= -INT_MAX and sign * tot <= INT_MAX else 0",
    "sign * tot if sign * tot >= INT_MIN and sign * tot <= INT_MAX else 0",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 9999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -9999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 42 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -42 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 123456789 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 12345 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -9876 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= -sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MIN else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else -INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MIN",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else -INT_MIN",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if -tot <= sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if -tot <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= -sign * tot else INT_MAX",
    "sign * tot if -sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -float('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= -INT_MAX else -INT_MAX",
    "sign * tot if sign * tot >= -sys.maxsize else -sys.maxsize",
    "sign * tot if sign * tot >= -float('inf') else -float('inf')",
    "sign * tot if sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if sign * tot <= float('inf') else float('inf')",
    "sign * tot if sign * tot >= -1e100 else -1e100",
    "sign * tot if sign * tot <= 1e100 else 1e100",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "sign * tot if -9999 <= sign * tot <= 9999 else 9999",
    "sign * tot if -10 <= sign * tot <= 10 else 10",
    "sign * tot if -1 <= sign * tot <= 1 else 1",
    "sign * tot if -0.5 <= sign * tot <= 0.5 else 0.5",
    "sign * tot if -0.1 <= sign * tot <= 0.1 else 0.1",
    "sign * tot if -0.01 <= sign * tot <= 0.01 else 0.01",
    "sign * tot if -0.001 <= sign * tot <= 0.001 else 0.001",
    "sign * tot if -0.0001 <= sign * tot <= 0.0001 else 0.0001",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else sign * tot",
    "sign * tot if __ <= sign * tot else sign * tot",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if __ <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -2 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -10 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1000 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= -INT_MAX else -INT_MAX",
    "sign * tot if -sign * tot >= -INT_MAX else -INT_MAX",
    "sign * tot if sign * tot <= sign * INT_MAX else sign * INT_MAX",
    "sign * tot if -sign * tot <= sign * INT_MAX else sign * INT_MAX",
    "sign * tot if sign * tot >= -sign * INT_MAX else -sign * INT_MAX",
    "sign * tot if -sign * tot >= -sign * INT_MAX else -sign * INT_MAX",
    "sign * tot if sign * tot <= sign * INT_MAX else sign * INT_MAX",
    "sign * tot if -sign * tot <= sign * INT_MAX else sign * INT_MAX",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -1e9 <= sign * tot <= 1e9 else 1e9",
    "sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "sign * tot if -999999 <= sign * tot <= 999999 else 999999",
    "sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "sign * tot if -999 <= sign * tot <= 999 else 999",
    "sign * tot if -100 <= sign * tot <= 100 else 100",
    "sign * tot if -99 <= sign * tot <= 99 else 99",
    "sign * tot if -10 <= sign * tot <= 10 else 10",
    "sign * tot if -9 <= sign * tot <= 9 else 9",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "sign * tot if -1e308 <= sign * tot <= 1e308 else 1e308",
    "sign * tot if -1e1000 <= sign * tot <= 1e1000 else 1e1000",
    "sign * tot if -1e10000 <= sign * tot <= 1e10000 else 1e10000",
    "sign * tot if -1e100000 <= sign * tot <= 1e100000 else 1e100000",
    "sign * tot if -1e1000000 <= sign * tot <= 1e1000000 else 1e1000000",
    "sign * tot if -1e10000000 <= sign * tot <= 1e10000000 else 1e10000000",
    "sign * tot if -1e100000000 <= sign * tot <= 1e100000000 else 1e100000000",
    "sign * tot if -INF <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -float('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1e100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -9999999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -987654321 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -42 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -10 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if sign * tot <= INT_MAX else sign * tot",
    "sign * tot if __ <= sign * tot <= INT_MAX else sign * tot <= INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else sign * tot <= sign * tot",
    "sign * tot if __ <= INT_MAX else sign * tot <= INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX else sign * tot <= sign * tot <= INT_MAX",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= sign * tot + sys.maxsize else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= sign * tot * sys.maxsize else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= sign * tot + sign * tot else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= INT_MAX else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= sign * tot + INT_MAX else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= sign * tot * INT_MAX else sys.maxsize",
    "sign * tot if -sign * tot <= sign * tot <= sign * tot + sign * tot else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "# Variant 1\nsign * tot if sign * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nsign * tot if __ <= INT_MAX else INT_MAX\n\n# Variant 3\nsign * tot if __ <= sign * tot else INT_MAX\n\n# Variant 4\nsign * tot if sign * tot <= __ else INT_MAX\n\n# Variant 5\nsign * tot if __ <= sign * tot <= __ else INT_MAX\n\n# Variant 6\nsign * tot if sign * tot <= INT_MAX <= __ else INT_MAX\n\n# Variant 7\nsign * tot if __ <= INT_MAX <= sign * tot else INT_MAX\n\n# Variant 8\nsign * tot if __ <= sign * tot <= INT_MAX else __\n\n# Variant 9\nsign * tot if __ <= INT_MAX <= __ else INT_MAX\n\n# Variant 10\nsign * tot if sign * tot <= __ <= INT_MAX else INT_MAX",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "sign * tot if -1e-100 <= sign * tot <= 1e-100 else 1e-100",
    "sign * tot if -math.inf <= sign * tot <= math.inf else math.inf",
    "sign * tot if -1e308 <= sign * tot <= 1e308 else 1e308",
    "sign * tot if -1e-308 <= sign * tot <= 1e-308 else 1e-308",
    "sign * tot if -1E100 <= sign * tot <= 1E100 else 1E100",
    "sign * tot if -1E-100 <= sign * tot <= 1E-100 else 1E-100",
    "sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= INT_MIN else INT_MIN",
    "sign * tot if sign * tot == 0 else INT_MAX",
    "sign * tot if sign * tot != 0 else INT_MAX",
    "sign * tot if sign * tot < 0 else INT_MAX",
    "sign * tot if sign * tot > 0 else INT_MAX",
    "sign * tot if sign * tot == sign * tot else INT_MAX",
    "sign * tot if sign * tot != sign * tot else INT_MAX",
    "sign * tot if sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot >= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else -INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if sign * tot <= INT_MAX else -INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else sign * tot",
    "sign * tot if sign * tot <= INT_MAX else 0",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else 0",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else sign * tot + 1",
    "sign * tot if sign * tot <= INT_MAX else sign * tot - 1",
    "sign * tot if True <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if False <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if None <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if \"\" <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if [] <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if {} <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if () <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -10 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1000 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1000 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if True else INT_MAX",
    "sign * tot if False else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot > sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot > INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot > INT_MAX else sign * tot",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if sign * tot <= sign * tot > INT_MAX else sign * tot <= INT_MAX",
    "sign * tot if sign * tot <= sign * tot > INT_MAX else sign * tot if sign * tot <= INT_MAX else sign * tot",
    "sign * tot if -sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX and sign * tot >= -sys.maxsize else INT_MAX",
    "sign * tot if -2147483648 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX and sign * tot >= -2147483648 else INT_MAX",
    "sign * tot if -9223372036854775808 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX and sign * tot >= -9223372036854775808 else INT_MAX",
    "sign * tot if -99999999999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX and sign * tot >= -99999999999 else INT_MAX",
    "sign * tot if -999999999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX and sign * tot >= -999999999 else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= (sign * tot) <= INT_MAX else INT_MAX",
    "sign * tot if (sign * tot) <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if (sign * tot) <= (sign * tot) <= INT_MAX else INT_MAX",
    "(sign * tot) if __ <= sign * tot <= INT_MAX else INT_MAX",
    "(sign * tot) if sign * tot <= INT_MAX else INT_MAX",
    "(sign * tot) if sign * tot <= (sign * tot) <= INT_MAX else INT_MAX",
    "(sign * tot) if (sign * tot) <= sign * tot <= INT_MAX else INT_MAX",
    "(sign * tot) if (sign * tot) <= (sign * tot) <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= -sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= -sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= -sign * tot <= -sign * tot <= -INT_MAX else INT_MAX",
    "# Variant 1\nsign * tot if True <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nsign * tot if False <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 3\nsign * tot if None <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 4\nsign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 5\nsign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 6\nsign * tot if 'string' <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 7\nsign * tot if [] <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 8\nsign * tot if {} <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 9\nsign * tot if () <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 10\nsign * tot if set() <= sign * tot <= INT_MAX else INT_MAX",
    "# Variant 1\nsign * tot if a <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nsign * tot if b <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 3\nsign * tot if c <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 4\nsign * tot if d <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 5\nsign * tot if e <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 6\nsign * tot if f <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 7\nsign * tot if g <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 8\nsign * tot if h <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 9\nsign * tot if i <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 10\nsign * tot if j <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= __ * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX else __",
    "sign * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX else __",
    "sign * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "sign * tot if sign * tot <= __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= __ <= __ else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= __ else INT_MAX",
    "sign * tot if __ <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= __ <= sign * tot else INT_MAX",
    "sign * tot if __ <= __ <= sign * tot else INT_MAX",
    "sign * tot if x <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if y <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if z <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if a <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if b <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if c <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if d <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if e <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if f <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if g <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "sign * tot if -1e9 <= sign * tot <= 1e9 else 1e9",
    "sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "sign * tot if -999999 <= sign * tot <= 999999 else 999999",
    "sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "sign * tot if -999 <= sign * tot <= 999 else 999",
    "sign * tot if -100 <= sign * tot <= 100 else 100",
    "sign * tot if -10 <= sign * tot <= 10 else 10",
    "sign * tot if -1 <= sign * tot <= 1 else 1",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= 0 else INT_MAX",
    "sign * tot if sign * tot <= 100 else INT_MAX",
    "sign * tot if sign * tot <= -100 else INT_MAX",
    "sign * tot if sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if INT_MIN <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= INT_MIN and sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= INT_MIN and sign * tot < INT_MAX else INT_MAX",
    "sign * tot if sign * tot > INT_MIN and sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if INT_MIN < sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if INT_MIN <= sign * tot < INT_MAX else INT_MAX",
    "sign * tot if sign * tot > INT_MIN and sign * tot < INT_MAX else INT_MAX",
    "sign * tot if INT_MIN < sign * tot < INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= INT_MIN and sign * tot > INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot else INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX",
    "sign * tot if sign * tot <= INT_MAX",
    "sign * tot if __ else INT_MAX",
    "sign * tot if __ <= sign * tot else sign * tot",
    "sign * tot if __ <= sign * tot <= INT_MAX else __",
    "sign * tot if __ <= sign * tot else __",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX <= INT_MAX <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= -INT_MAX and sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot >= -INT_MAX and sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -2147483648 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 2147483647 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -2147483649 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1000 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 500 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -9999 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 10000 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INF <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if NEG_INF <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if NEG_INFINITY <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if MIN_INT <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if MINIMUM_INT <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if MINIMUM_INTEGER <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if NEGATIVE_INFINITY <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -float('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if float('-inf') <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -1e100 <= sign * tot <= INT_MAX else INT_MAX",
    "# Variant 1\nsign * tot if -INF <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nsign * tot if -10 <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 3\nsign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 4\nsign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 5\nsign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 6\nsign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 7\nsign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 8\nsign * tot if -sign * tot <= sign * tot <= 100 else INT_MAX\n\n# Variant 9\nsign * tot if -sign * tot <= sign * tot <= 0 else INT_MAX\n\n# Variant 10\nsign * tot if -sign * tot <= sign * tot <= -10 else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= -sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= -INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MIN else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else -INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MIN",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else -INT_MIN",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else -INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else tot",
    "sign * tot if x <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if y <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if z <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if a <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if b <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if c <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if d <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if e <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if f <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if g <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot <= sign else INT_MAX",
    "sign * tot if __ <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "sign * tot if sign * tot <= sign * tot else INT_MAX",
    "sign * tot if True <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if False <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if None <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 'hello' <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if [] <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if {} <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if () <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if object() <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= sign * tot <= -sign * tot else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= -sign * tot else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= -INT_MAX <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= -sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -INT_MAX <= -INT_MAX <= -sign * tot else INT_MAX",
    "sign * tot if -sign * tot <= -sign * tot <= -sign * tot else INT_MAX",
    "sign * tot if sign * tot <= -INT_MAX <= INT_MAX else INT_MAX",
    "sign * tot if a <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if b <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if c <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if d <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if e <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if f <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if g <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if h <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if i <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if j <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "sign * tot if __ <= sign * tot else INT_MAX",
    "sign * tot if __ <= sign * tot <= INT_MAX else __",
    "sign * tot if __ <= sign * tot <= INT_MAX else None",
    "sign * tot if __ <= sign * tot <= INT_MAX else 0",
    "sign * tot if __ <= sign * tot <= INT_MAX else -1",
    "sign * tot if __ <= sign * tot <= INT_MAX else sign * tot",
    "sign * tot if __ <= sign * tot <= INT_MAX else tot",
    "sign * tot if __ <= sign * tot <= INT_MAX else sign",
    "sign * tot if __ <= sign * tot <= INT_MAX else 42"
  ],
  "num_code_blocks": 555
}
