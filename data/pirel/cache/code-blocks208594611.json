{
  "template": "[matrix[top][__] for __ in range(left, right + 1)]",
  "lang": "py",
  "temperature": 0.7,
  "code_blocks": [
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][col_idx] for col_idx in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][m] for m in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[__][i] for i in range(left, right + 1)]",
    "[matrix[__][j] for j in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[row][__] for row in range(top, bottom + 1)]",
    "[matrix[row][j] for row in range(top, bottom + 1)]",
    "[matrix[__][index] for index in range(left, right + 1)]",
    "[matrix[row][index] for row in range(top, bottom + 1)]",
    "[matrix[row][col] for row in range(top, bottom + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right+1)]",
    "[matrix[top][j] for j in range(left, right+1)]",
    "[matrix[top][idx] for idx in range(left, right+1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][m] for m in range(left, right + 1)]",
    "[matrix[top][y] for y in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][m] for m in range(left, right + 1)]",
    "[matrix[top][element] for element in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col_idx] for col_idx in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][y] for y in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][val] for val in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][0] for _ in range(left, right + 1)]",
    "[matrix[top][1] for _ in range(left, right + 1)]",
    "[matrix[top][2] for _ in range(left, right + 1)]",
    "[matrix[top][3] for _ in range(left, right + 1)]",
    "[matrix[top][4] for _ in range(left, right + 1)]",
    "[matrix[top][5] for _ in range(left, right + 1)]",
    "[matrix[top][6] for _ in range(left, right + 1)]",
    "[matrix[top][7] for _ in range(left, right + 1)]",
    "[matrix[top][8] for _ in range(left, right + 1)]",
    "[matrix[top][9] for _ in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][y] for y in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col_index] for col_index in range(left, right + 1)]",
    "[matrix[top][element] for element in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][val] for val in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][position] for position in range(left, right + 1)]",
    "[matrix[top][element] for element in range(left, right + 1)]",
    "[matrix[top][val] for val in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][var] for var in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][y] for y in range(left, right + 1)]",
    "[matrix[top][element] for element in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][element] for element in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][val] for val in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][y] for y in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][cell] for cell in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][m] for m in range(left, right + 1)]",
    "[matrix[top][p] for p in range(left, right + 1)]",
    "[matrix[top][q] for q in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][column] for column in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][m] for m in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][elem] for elem in range(left, right + 1)]",
    "[matrix[top][0] for _ in range(left, right + 1)]",
    "[matrix[top][1] for _ in range(left, right + 1)]",
    "[matrix[top][2] for _ in range(left, right + 1)]",
    "[matrix[top][3] for _ in range(left, right + 1)]",
    "[matrix[top][4] for _ in range(left, right + 1)]",
    "[matrix[top][5] for _ in range(left, right + 1)]",
    "[matrix[top][6] for _ in range(left, right + 1)]",
    "[matrix[top][7] for _ in range(left, right + 1)]",
    "[matrix[top][8] for _ in range(left, right + 1)]",
    "[matrix[top][9] for _ in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][element] for element in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][m] for m in range(left, right + 1)]",
    "[matrix[top][p] for p in range(left, right + 1)]",
    "[matrix[top][q] for q in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][start] for start in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][pos] for pos in range(left, right + 1)]",
    "[matrix[top][num] for num in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][value] for value in range(left, right + 1)]",
    "[matrix[top][i] for i in range(left, right + 1)]",
    "[matrix[top][j] for j in range(left, right + 1)]",
    "[matrix[top][x] for x in range(left, right + 1)]",
    "[matrix[top][idx] for idx in range(left, right + 1)]",
    "[matrix[top][n] for n in range(left, right + 1)]",
    "[matrix[top][col] for col in range(left, right + 1)]",
    "[matrix[top][k] for k in range(left, right + 1)]",
    "[matrix[top][m] for m in range(left, right + 1)]",
    "[matrix[top][y] for y in range(left, right + 1)]",
    "[matrix[top][index] for index in range(left, right + 1)]"
  ],
  "num_code_blocks": 300
}
