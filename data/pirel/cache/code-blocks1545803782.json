{
  "template": "return sign * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
  "lang": "py",
  "temperature": 0.7,
  "code_blocks": [
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "return sign * tot if -1e308 <= sign * tot <= 1e308 else 1e308",
    "limit = 10**100\nreturn sign * tot if -limit <= sign * tot <= limit else limit",
    "limit = 1e1000\nreturn sign * tot if -limit <= sign * tot <= limit else limit",
    "limit = 1e308\nreturn sign * tot if -limit <= sign * tot <= limit else limit",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot >= -float('inf') and sign * tot <= float('inf') else float('inf')",
    "return sign * tot if sign * tot >= -1e100 and sign * tot <= 1e100 else 1e100",
    "return sign * tot if -sys.maxsize - 1 <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -1000000000 <= sign * tot <= 1000000000 else 1000000000",
    "return sign * tot if -9999 <= sign * tot <= 9999 else 9999",
    "return sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "return sign * tot if -999 <= sign * tot <= 999 else 999",
    "return sign * tot if -100 <= sign * tot <= 100 else 100",
    "return sign * tot if -99 <= sign * tot <= 99 else 99",
    "return sign * tot if -10 <= sign * tot <= 10 else 10",
    "return sign * tot if -9 <= sign * tot <= 9 else 9",
    "return sign * tot if -1 <= sign * tot <= 1 else 1",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e9 <= sign * tot <= 1e9 else 1e9",
    "return sign * tot if -999999999 <= sign * tot <= 999999999 else 999999999",
    "return sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "return sign * tot if -100000 <= sign * tot <= 100000 else 100000",
    "return sign * tot if -10000 <= sign * tot <= 10000 else 10000",
    "return sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "return sign * tot if -100 <= sign * tot <= 100 else 100",
    "return sign * tot if -10 <= sign * tot <= 10 else 10",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -1e9 <= sign * tot <= 1e9 else 1e9",
    "return sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "return sign * tot if -999999 <= sign * tot <= 999999 else 999999",
    "return sign * tot if -10000 <= sign * tot <= 10000 else 10000",
    "return sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "return sign * tot if -100 <= sign * tot <= 100 else 100",
    "return sign * tot if -10 <= sign * tot <= 10 else 10",
    "return sign * tot if -1 <= sign * tot <= 1 else 1",
    "return sign * tot if -0.1 <= sign * tot <= 0.1 else 0.1",
    "return sign * tot if -2 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot + 1 <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot - 1 <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * (tot + 1) <= INT_MAX else INT_MAX",
    "return sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 3 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 4 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -2 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 5 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if None <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if True <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if False <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0.0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0.1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -0.1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 'hello' <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "return sign * tot if -10**100 <= sign * tot <= 10**100 else 10**100",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= sys.float_info.max else sys.float_info.max",
    "return sign * tot if -float('infinity') <= sign * tot <= float('infinity') else float('infinity')",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if sign * tot <= INT_MAX and __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX if __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX if __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot if sign * tot <= INT_MAX else INT_MAX if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX if __ <= sign * tot else INT_MAX if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return sign * tot if sign * tot <= __ <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if sign * tot <= INT_MAX <= __ else INT_MAX",
    "return sign * tot if __ <= INT_MAX <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= __ <= INT_MAX else INT_MAX",
    "return sign * tot if -2147483648 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 2147483647 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -12345 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -98765 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 55555 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -99999 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1234567890 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else sys.maxsize",
    "return sign * tot if -1e9 <= sign * tot <= 1e9 else sys.maxsize",
    "return sign * tot if -1000000000 <= sign * tot <= 1000000000 else sys.maxsize",
    "return sign * tot if -10**9 <= sign * tot <= 10**9 else sys.maxsize",
    "return sign * tot if -1_000_000_000 <= sign * tot <= 1_000_000_000 else sys.maxsize",
    "return sign * tot if -1e9 <= sign * tot <= 1e9 else sys.maxsize",
    "return sign * tot if -1000000000 <= sign * tot <= 1000000000 else sys.maxsize",
    "return sign * tot if -10**9 <= sign * tot <= 10**9 else sys.maxsize",
    "return sign * tot if -1_000_000_000 <= sign * tot <= 1_000_000_000 else sys.maxsize",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else __",
    "return sign * tot if tot <= sign * tot else __",
    "return sign * tot if __ <= sign * tot else __",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return sign * tot if tot <= sign * tot <= __ else INT_MAX",
    "return sign * tot if sign * tot <= __ else __",
    "return sign * tot if -sys.maxsize - 1 <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -9223372036854775807 <= sign * tot <= 9223372036854775807 else 9223372036854775807",
    "return sign * tot if -2147483648 <= sign * tot <= 2147483647 else 2147483647",
    "return sign * tot if -32768 <= sign * tot <= 32767 else 32767",
    "return sign * tot if -128 <= sign * tot <= 127 else 127",
    "return sign * tot if -3.4028234663852886e+38 <= sign * tot <= 3.4028234663852886e+38 else 3.4028234663852886e+38",
    "return sign * tot if -3.4028235e+38 <= sign * tot <= 3.4028235e+38 else 3.4028235e+38",
    "return sign * tot if -sys.maxsize - 1 <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize - 1 else sys.maxsize",
    "return sign * tot if sign * tot <= sys.maxsize - 1 else sys.maxsize",
    "return sign * tot if -sys.maxsize - 1 <= sign * tot <= sys.maxsize - 1 else sys.maxsize",
    "return sign * tot if sign * tot <= sys.maxsize - 1 else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if sign * tot <= sign * sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot <= sign * sys.maxsize - 1 else sys.maxsize",
    "return sign * tot if sign * tot <= sign * sys.maxsize else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -10e10 <= sign * tot <= 10e10 else 10e10",
    "return sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "return sign * tot if -999999 <= sign * tot <= 999999 else 999999",
    "return sign * tot if -100 <= sign * tot <= 100 else 100",
    "return sign * tot if -1 <= sign * tot <= 1 else 1",
    "return sign * tot if -0.001 <= sign * tot <= 0.001 else 0.001",
    "return sign * tot if -999.99 <= sign * tot <= 999.99 else 999.99",
    "return sign * tot if -10 ** 6 <= sign * tot <= 10 ** 6 else 10 ** 6",
    "return sign * tot if -2 ** 31 <= sign * tot <= 2 ** 31 - 1 else 2 ** 31 - 1",
    "return sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= -INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else -INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else sign * tot",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else sign * -tot",
    "return sign * tot if sign * tot <= sign * tot <= sign * -tot else INT_MAX",
    "return sign * tot if sign * tot <= sign * -tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * -tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= tot <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= tot <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= __ <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= sign * tot <= __ else INT_MAX",
    "return sign * tot if tot <= sign * tot else INT_MAX",
    "return sign * tot if -2147483648 <= sign * tot <= 2147483647 else 2147483647",
    "return sign * tot if -2_147_483_648 <= sign * tot <= 2_147_483_647 else 2_147_483_647",
    "return sign * tot if -int(1e9) <= sign * tot <= int(1e9) else int(1e9)",
    "return sign * tot if -1_000_000_000 <= sign * tot <= 1_000_000_000 else 1_000_000_000",
    "return sign * tot if -1e9 <= sign * tot <= 1e9 else 1e9",
    "return sign * tot if -1000000000 <= sign * tot <= 1000000000 else 1000000000",
    "return sign * tot if -10**9 <= sign * tot <= 10**9 else 10**9",
    "return sign * tot if -1000000000 <= sign * tot <= 1000000000 else 1000000000",
    "return sign * tot if sign * tot >= -2147483648 and sign * tot <= 2147483647 else 2147483647",
    "return sign * tot if -2147483648 <= sign * tot <= 2147483647 else 2147483647",
    "return sign * tot if -INF <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if None <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if float('nan') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 42 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if False <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 'abc' <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if [] <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if {} <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= sign * tot else __",
    "return sign * tot if sign * tot <= __ else __",
    "return sign * tot if __ <= sign * tot <= __ else __",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 9999 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -9999 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 42 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -42 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 123456789 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return __ * tot if __ <= __ * tot <= INT_MAX else INT_MAX",
    "return __ * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= __ * tot else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else __",
    "return __ * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
    "return __ * tot if sign * tot <= INT_MAX else __",
    "return sign * __ if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return sign * tot if sign * tot <= __ else __",
    "return sign * tot if __ <= __ <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= __ else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= __ else __",
    "return sign * tot if -float('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= -INT_MAX and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if abs(sign * tot) <= INT_MAX else INT_MAX",
    "return sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= -INT_MAX and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= -sys.maxsize - 1 and sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if sign * tot >= -sys.maxsize - 1 and sign * tot <= sys.maxsize + 1 else INT_MAX",
    "return sign * tot if sign * tot >= -sys.maxsize - 1 and sign * tot <= sys.maxsize - 1 else INT_MAX",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN else INT_MIN",
    "return sign * tot if sign * tot >= INT_MIN and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN or sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN and sign * tot >= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MIN or sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MIN and sign * tot >= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN or sign * tot >= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MIN and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN and sign * tot <= INT_MAX else INT_MIN",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= __ <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= sign * tot <= INT_MAX else sign * tot",
    "return __ * tot if __ <= __ * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * __ <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return __ * tot if __ <= __ * tot <= __ else INT_MAX",
    "return __ * tot if __ <= __ * tot <= INT_MAX else __",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot else __",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return sign * tot if sign * tot <= __ <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= __ <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= sign * tot else __",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -math.inf <= sign * tot <= math.inf else math.inf",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -3.4028235e+38 <= sign * tot <= 3.4028235e+38 else 3.4028235e+38",
    "return sign * tot if -2147483648 <= sign * tot <= 2147483647 else 2147483647",
    "return sign * tot if -9223372036854775808 <= sign * tot <= 9223372036854775807 else 9223372036854775807",
    "return sign * tot if -1.157920892373162e+77 <= sign * tot <= 1.157920892373162e+77 else 1.157920892373162e+77",
    "return sign * tot if -1.3407807929942597e+154 <= sign * tot <= 1.3407807929942597e+154 else 1.3407807929942597e+154",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot + 100 else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot - 100 else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot + sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot - sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX - sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX and __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and sign * tot >= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and __ <= tot * sign else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and sign * tot >= __ else INT_MAX",
    "return sign * tot if sign * tot >= __ and __ <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= __ <= INT_MAX and __ >= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and __ <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot >= __ <= INT_MAX and __ >= tot * sign else INT_MAX",
    "return sign * tot if sign * tot >= __ and __ <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -float('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1e100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -math.inf <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -decimal.Decimal('Infinity') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -Fraction('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -np.inf <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -float('-inf') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -float('-1e400') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -float('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= -INT_MAX and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= -float('inf') and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= -INT_MAX and sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if sign * tot >= -float('inf') and sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if -INT_MAX <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else -INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= -INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX * sign",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else sign * INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else -sign * INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX + sign",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX - sign",
    "return sign * tot if x <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if y <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if z <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if a <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if b <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if c <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if val <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if num <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if result <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if value <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= sign * tot <= INT_MAX else None",
    "return sign * tot if sign * tot <= INT_MAX else None",
    "return sign * tot if __ <= sign * tot else None",
    "return sign * tot if sign * tot <= INT_MAX else 0",
    "return sign * tot if __ <= sign * tot else 0",
    "return sign * tot if __ <= sign * tot <= INT_MAX else 0",
    "return sign * tot if sign * tot <= INT_MAX else 0",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= sign * tot else INT_MAX",
    "return sign * tot if __ <= tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= INT_MAX else __",
    "return sign * tot if sign * tot <= __ else __",
    "return sign * tot if __ <= sign * tot else __",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= INT_MAX <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= tot <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= sign * tot <= INT_MAX <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= tot <= INT_MAX <= INT_MAX else INT_MAX",
    "return sign * tot if tot <= tot <= INT_MAX <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX and INT_MAX else INT_MAX",
    "return sign * tot if tot <= sign * tot <= INT_MAX and INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sys.maxsize else INT_MAX",
    "return sign * tot if sign * tot <= sys.maxsize <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else sign * tot",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -2 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -10 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -float('inf') <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1e100 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -math.inf <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -9999999999999999 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize - 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1e308 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -float('inf') - 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1.7976931348623157e+308 - 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if False <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if True <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if None <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0.0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1.0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -1.0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if '' <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN else INT_MIN",
    "return sign * tot if sign * tot < INT_MAX else INT_MAX",
    "return sign * tot if sign * tot > INT_MIN else INT_MIN",
    "return sign * tot if sign * tot == INT_MAX else INT_MAX",
    "return sign * tot if sign * tot != INT_MIN else INT_MIN",
    "return sign * tot if sign * tot <= sign * tot else sign * tot",
    "return sign * tot if sign * tot >= sign * tot else sign * tot",
    "return sign * tot if sign * tot < sign * tot else sign * tot",
    "return sign * tot if sign * tot > sign * tot else sign * tot",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 10 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= INT_MIN else INT_MIN",
    "return sign * tot if sign * tot >= -INT_MAX else -INT_MAX",
    "return sign * tot if sign * tot >= -1 * INT_MAX else -1 * INT_MAX",
    "return sign * tot if sign * tot >= -10 * INT_MAX else -10 * INT_MAX",
    "return sign * tot if n <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if val <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if result <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if number <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if value <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if x <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if limit <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if res <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if output <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if value <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if -sys.maxsize - 1 <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if (-sys.maxsize - 1) <= (sign * tot) <= sys.maxsize else sys.maxsize",
    "return sign * tot if (sign * tot >= -sys.maxsize - 1) and (sign * tot <= sys.maxsize) else sys.maxsize",
    "return sign * tot if (-sys.maxsize - 1 <= sign * tot) and (sign * tot <= sys.maxsize) else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize - 1 and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if (-sys.maxsize - 1 <= sign * tot <= sys.maxsize) else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize - 1 <= sys.maxsize else sys.maxsize",
    "return sign * tot if (-sys.maxsize - 1 <= sign * tot <= sys.maxsize) else sys.maxsize",
    "return sign * tot if -sys.maxsize - 1 <= sign * tot and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize - 1 and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * __ <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= __ else INT_MAX",
    "return __ * tot if __ <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * __ <= INT_MAX else INT_MAX",
    "return __ * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * __ if __ <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= __ <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if sign * tot <= INT_MAX and sign * tot >= __ else INT_MAX",
    "return sign * tot if __ <= sign * tot and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and sign * tot >= -INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= __ and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and sign * tot >= INT_MIN else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX and sign * tot >= -__ else INT_MAX",
    "return sign * tot if __ <= sign * tot and sign * tot <= INT_MAX and sign * tot >= -INT_MAX else INT_MAX",
    "return sign * tot if sign * tot >= __ and sign * tot <= INT_MAX and sign * tot >= -INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX and sign * tot >= -INT_MAX and sign * tot >= INT_MIN else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX and sign * tot >= -INT_MAX and sign * tot >= __ else INT_MAX",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -(2**31) <= sign * tot <= (2**31)-1 else (2**31)-1",
    "return sign * tot if -1e10 <= sign * tot <= 1e10 else 1e10",
    "return sign * tot if -math.inf <= sign * tot <= math.inf else math.inf",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -9223372036854775808 <= sign * tot <= 9223372036854775807 else 9223372036854775807",
    "return sign * tot if -3.4028235e+38 <= sign * tot <= 3.4028235e+38 else 3.4028235e+38",
    "return sign * tot if -1.17549435e-38 <= sign * tot <= 1.17549435e-38 else 1.17549435e-38",
    "return sign * tot if -1.7976931348623157e+308 <= sign * tot <= 1.7976931348623157e+308 else 1.7976931348623157e+308",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e9 <= sign * tot <= 1e9 else 1e9",
    "return sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "return sign * tot if -999999 <= sign * tot <= 999999 else 999999",
    "return sign * tot if -100000 <= sign * tot <= 100000 else 100000",
    "return sign * tot if -99999 <= sign * tot <= 99999 else 99999",
    "return sign * tot if -10000 <= sign * tot <= 10000 else 10000",
    "return sign * tot if -9999 <= sign * tot <= 9999 else 9999",
    "return sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -1e9 <= sign * tot <= 1e9 else 1e9",
    "return sign * tot if -1000000 <= sign * tot <= 1000000 else 1000000",
    "return sign * tot if -10000 <= sign * tot <= 10000 else 10000",
    "return sign * tot if -1000 <= sign * tot <= 1000 else 1000",
    "return sign * tot if -100 <= sign * tot <= 100 else 100",
    "return sign * tot if -10 <= sign * tot <= 10 else 10",
    "return sign * tot if -5 <= sign * tot <= 5 else 5",
    "return sign * tot if -2 <= sign * tot <= 2 else 2",
    "return sign * tot if -1 <= sign * tot <= 1 else 1",
    "return sign * tot if sign * tot >= __ and sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if __ <= sign * tot <= INT_MAX else __",
    "return sign * tot if __ <= sign * tot <= INT_MAX else sign * tot",
    "return sign * tot if sign * tot >= __ and sign * tot <= INT_MAX else sign * tot",
    "return sign * tot if __ <= sign * tot <= INT_MAX else None",
    "return sign * tot if __ <= sign * tot <= INT_MAX else sign",
    "return sign * tot if sign * tot >= __ and sign * tot <= INT_MAX else 0",
    "return sign * tot if __ <= sign * tot <= INT_MAX else -1",
    "return sign * tot if __ <= sign * tot <= INT_MAX else tot",
    "return sign * tot if sign * tot >= __ and sign * tot <= INT_MAX else tot",
    "",
    "",
    "",
    "",
    "",
    "",
    "",
    "",
    "",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if sign * tot >= -sys.maxsize and sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -sys.maxsize <= sign * tot <= sys.maxsize else sys.maxsize",
    "return sign * tot if -math.inf <= sign * tot <= math.inf else math.inf",
    "return sign * tot if -float('inf') <= sign * tot <= float('inf') else float('inf')",
    "return sign * tot if -1e100 <= sign * tot <= 1e100 else 1e100",
    "return sign * tot if -10000000000 <= sign * tot <= 10000000000 else 10000000000",
    "return sign * tot if -9999999999 <= sign * tot <= 9999999999 else 9999999999",
    "return sign * tot if -999999999 <= sign * tot <= 999999999 else 999999999",
    "return sign * tot if -123456789 <= sign * tot <= 123456789 else 123456789",
    "return sign * tot if -100000000 <= sign * tot <= 100000000 else 100000000",
    "return sign * tot if -10000000 <= sign * tot <= 10000000 else 10000000",
    "return sign * tot if sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= sign * tot <= sign * tot else sign * tot",
    "return sign * tot if INT_MAX >= sign * tot >= sign * tot else INT_MAX",
    "return sign * tot if INT_MAX >= sign * tot >= INT_MAX else INT_MAX",
    "return sign * tot if INT_MAX >= sign * tot >= INT_MAX else sign * tot",
    "return sign * tot if INT_MAX >= sign * tot >= sign * tot else sign * tot",
    "return sign * tot if sign * tot <= INT_MAX <= sign * tot else INT_MAX",
    "return sign * tot if sign * tot <= INT_MAX <= INT_MAX else INT_MAX",
    "# Variant 1\nreturn sign * tot if -sign * tot <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 2\nreturn sign * tot if sign * tot <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 3\nreturn sign * tot if -tot <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 4\nreturn sign * tot if tot <= sign * tot <= INT_MAX else INT_MAX\n\n# Variant 5\nreturn sign * tot if -sign * tot <= -sign * tot <= INT_MAX else INT_MAX\n\n# Variant 6\nreturn sign * tot if sign * tot <= -sign * tot <= INT_MAX else INT_MAX\n\n# Variant 7\nreturn sign * tot if -tot <= -sign * tot <= INT_MAX else INT_MAX\n\n# Variant 8\nreturn sign * tot if tot <= -sign * tot <= INT_MAX else INT_MAX\n\n# Variant 9\nreturn sign * tot if -sign * tot <= sign * tot <= INT_MAX else -INT_MAX\n\n# Variant 10\nreturn sign * tot if sign * tot <= sign * tot <= -INT_MAX else INT_MAX",
    "return sign * tot if -1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 0 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 1 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 2 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 3 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 4 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 5 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 6 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 7 <= sign * tot <= INT_MAX else INT_MAX",
    "return sign * tot if 8 <= sign * tot <= INT_MAX else INT_MAX"
  ],
  "num_code_blocks": 590
}
