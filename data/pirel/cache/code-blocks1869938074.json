{
  "template": "__ * tot if INT_MIN <= __ * tot <= INT_MAX else INT_MAX",
  "lang": "py",
  "temperature": 0.7,
  "code_blocks": [
    "int_1 * tot if INT_MIN <= int_1 * tot <= INT_MAX else INT_MAX",
    "num_1 * tot if INT_MIN <= num_1 * tot <= INT_MAX else INT_MAX",
    "value_1 * tot if INT_MIN <= value_1 * tot <= INT_MAX else INT_MAX",
    "x_1 * tot if INT_MIN <= x_1 * tot <= INT_MAX else INT_MAX",
    "abc_1 * tot if INT_MIN <= abc_1 * tot <= INT_MAX else INT_MAX",
    "value_2 * tot if INT_MIN <= value_2 * tot <= INT_MAX else INT_MAX",
    "x_2 * tot if INT_MIN <= x_2 * tot <= INT_MAX else INT_MAX",
    "abc_2 * tot if INT_MIN <= abc_2 * tot <= INT_MAX else INT_MAX",
    "num_2 * tot if INT_MIN <= num_2 * tot <= INT_MAX else INT_MAX",
    "int_2 * tot if INT_MIN <= int_2 * tot <= INT_MAX else INT_MAX",
    "-1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "tot * tot if INT_MIN <= tot * tot <= INT_MAX else INT_MAX",
    "100 * tot if INT_MIN <= 100 * tot <= INT_MAX else INT_MAX",
    "-5 * tot if INT_MIN <= -5 * tot <= INT_MAX else INT_MAX",
    "7 * tot if INT_MIN <= 7 * tot <= INT_MAX else INT_MAX",
    "tot * 10 if INT_MIN <= tot * 10 <= INT_MAX else INT_MAX",
    "tot * -2 if INT_MIN <= tot * -2 <= INT_MAX else INT_MAX",
    "tot * 0 if INT_MIN <= tot * 0 <= INT_MAX else INT_MAX",
    "1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX",
    "4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX",
    "5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "6 * tot if INT_MIN <= 6 * tot <= INT_MAX else INT_MAX",
    "7 * tot if INT_MIN <= 7 * tot <= INT_MAX else INT_MAX",
    "8 * tot if INT_MIN <= 8 * tot <= INT_MAX else INT_MAX",
    "9 * tot if INT_MIN <= 9 * tot <= INT_MAX else INT_MAX",
    "10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "1. a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "2. b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "3. c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "4. d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "5. e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "6. f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "7. g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "8. h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "9. i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "10. j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "-1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "100 * tot if INT_MIN <= 100 * tot <= INT_MAX else INT_MAX",
    "-5 * tot if INT_MIN <= -5 * tot <= INT_MAX else INT_MAX",
    "10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "-100 * tot if INT_MIN <= -100 * tot <= INT_MAX else INT_MAX",
    "1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "-10 * tot if INT_MIN <= -10 * tot <= INT_MAX else INT_MAX",
    "5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "# Variant 1\nx * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX\n\n# Variant 2\ny * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX\n\n# Variant 3\nz * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX\n\n# Variant 4\na * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX\n\n# Variant 5\nb * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX\n\n# Variant 6\nc * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX\n\n# Variant 7\nd * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX\n\n# Variant 8\ne * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX\n\n# Variant 9\nf * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX\n\n# Variant 10\ng * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "1. a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "2. b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "3. c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "4. d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "5. e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "6. f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "7. g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "8. h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "9. i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "10. j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX",
    "i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "1. 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX\n\n2. 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX\n\n3. 3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX\n\n4. 4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX\n\n5. 5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX\n\n6. 6 * tot if INT_MIN <= 6 * tot <= INT_MAX else INT_MAX\n\n7. 7 * tot if INT_MIN <= 7 * tot <= INT_MAX else INT_MAX\n\n8. 8 * tot if INT_MIN <= 8 * tot <= INT_MAX else INT_MAX\n\n9. 9 * tot if INT_MIN <= 9 * tot <= INT_MAX else INT_MAX\n\n10. 10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "1. (a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX)\n2. (b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX)\n3. (c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX)\n4. (d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX)\n5. (e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX)\n6. (f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX)\n7. (g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX)\n8. (h * tot if INT_MIN <= h * tot <= INT_MAX else INT_MAX)\n9. (i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX)\n10. (j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX)",
    "-1",
    "2",
    "0",
    "100",
    "-100",
    "1",
    "10",
    "-10",
    "42",
    "999",
    "1. x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX\n2. y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX\n3. z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX\n4. a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX\n5. b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX\n6. c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX\n7. num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX\n8. val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX\n9. count * tot if INT_MIN <= count * tot <= INT_MAX else INT_MAX\n10. result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX",
    "1. (2 * tot) if INT_MIN <= (2 * tot) <= INT_MAX else INT_MAX",
    "2. (num * tot) if INT_MIN <= (num * tot) <= INT_MAX else INT_MAX",
    "3. (x * tot) if INT_MIN <= (x * tot) <= INT_MAX else INT_MAX",
    "4. (value * tot) if INT_MIN <= (value * tot) <= INT_MAX else INT_MAX",
    "5. (n * tot) if INT_MIN <= (n * tot) <= INT_MAX else INT_MAX",
    "6. (count * tot) if INT_MIN <= (count * tot) <= INT_MAX else INT_MAX",
    "7. (result * tot) if INT_MIN <= (result * tot) <= INT_MAX else INT_MAX",
    "8. (product * tot) if INT_MIN <= (product * tot) <= INT_MAX else INT_MAX",
    "9. (value1 * tot) if INT_MIN <= (value1 * tot) <= INT_MAX else INT_MAX",
    "10. (y * tot) if INT_MIN <= (y * tot) <= INT_MAX else INT_MAX",
    "x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "1. x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX\n\n2. num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX\n\n3. value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX\n\n4. n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX\n\n5. val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX\n\n6. number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX\n\n7. multiplier * tot if INT_MIN <= multiplier * tot <= INT_MAX else INT_MAX\n\n8. result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX\n\n9. var * tot if INT_MIN <= var * tot <= INT_MAX else INT_MAX\n\n10. product * tot if INT_MIN <= product * tot <= INT_MAX else INT_MAX",
    "1. (2 + 3) * tot if INT_MIN <= (2 + 3) * tot <= INT_MAX else INT_MAX\n\n2. my_var * tot if INT_MIN <= my_var * tot <= INT_MAX else INT_MAX\n\n3. x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX\n\n4. (a - b) * tot if INT_MIN <= (a - b) * tot <= INT_MAX else INT_MAX\n\n5. 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX\n\n6. (x + y) * tot if INT_MIN <= (x + y) * tot <= INT_MAX else INT_MAX\n\n7. (4 / 2) * tot if INT_MIN <= (4 / 2) * tot <= INT_MAX else INT_MAX\n\n8. (num - 1) * tot if INT_MIN <= (num - 1) * tot <= INT_MAX else INT_MAX\n\n9. (num + 1) * tot if INT_MIN <= (num + 1) * tot <= INT_MAX else INT_MAX\n\n10. (a * b) * tot if INT_MIN <= (a * b) * tot <= INT_MAX else INT_MAX",
    "1. (a + b) * tot if INT_MIN <= (a + b) * tot <= INT_MAX else INT_MAX\n\n2. (x - y) * tot if INT_MIN <= (x - y) * tot <= INT_MAX else INT_MAX\n\n3. (num1 / num2) * tot if INT_MIN <= (num1 / num2) * tot <= INT_MAX else INT_MAX\n\n4. (var1 * var2) * tot if INT_MIN <= (var1 * var2) * tot <= INT_MAX else INT_MAX\n\n5. (value + 1) * tot if INT_MIN <= (value + 1) * tot <= INT_MAX else INT_MAX\n\n6. (count - 2) * tot if INT_MIN <= (count - 2) * tot <= INT_MAX else INT_MAX\n\n7. (factor ** power) * tot if INT_MIN <= (factor ** power) * tot <= INT_MAX else INT_MAX\n\n8. (length // width) * tot if INT_MIN <= (length // width) * tot <= INT_MAX else INT_MAX\n\n9. (result % 10) * tot if INT_MIN <= (result % 10) * tot <= INT_MAX else INT_MAX\n\n10. (n * m) * tot if INT_MIN <= (n * m) * tot <= INT_MAX else INT_MAX",
    "1. 1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX\n\n2. 2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX\n\n3. 3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX\n\n4. 4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX\n\n5. 5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX\n\n6. 6 * tot if INT_MIN <= 6 * tot <= INT_MAX else INT_MAX\n\n7. 7 * tot if INT_MIN <= 7 * tot <= INT_MAX else INT_MAX\n\n8. 8 * tot if INT_MIN <= 8 * tot <= INT_MAX else INT_MAX\n\n9. 9 * tot if INT_MIN <= 9 * tot <= INT_MAX else INT_MAX\n\n10. 10 * tot if INT_MIN <= 10 * tot <= INT_MAX else INT_MAX",
    "1. x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX\n\n2. y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX\n\n3. z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX\n\n4. a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX\n\n5. b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX\n\n6. c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX\n\n7. i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX\n\n8. j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX\n\n9. k * tot if INT_MIN <= k * tot <= INT_MAX else INT_MAX\n\n10. n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "1. (2 * tot) if INT_MIN <= (2 * tot) <= INT_MAX else INT_MAX\n\n2. (x * tot) if INT_MIN <= (x * tot) <= INT_MAX else INT_MAX\n\n3. (n * tot) if INT_MIN <= (n * tot) <= INT_MAX else INT_MAX\n\n4. (val * tot) if INT_MIN <= (val * tot) <= INT_MAX else INT_MAX\n\n5. (a * tot) if INT_MIN <= (a * tot) <= INT_MAX else INT_MAX\n\n6. (num * tot) if INT_MIN <= (num * tot) <= INT_MAX else INT_MAX\n\n7. (count * tot) if INT_MIN <= (count * tot) <= INT_MAX else INT_MAX\n\n8. (factor * tot) if INT_MIN <= (factor * tot) <= INT_MAX else INT_MAX\n\n9. (multiplier * tot) if INT_MIN <= (multiplier * tot) <= INT_MAX else INT_MAX\n\n10. (product * tot) if INT_MIN <= (product * tot) <= INT_MAX else INT_MAX",
    "1. x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "2. y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "3. z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "4. a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "5. b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "6. c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "7. i * tot if INT_MIN <= i * tot <= INT_MAX else INT_MAX",
    "8. j * tot if INT_MIN <= j * tot <= INT_MAX else INT_MAX",
    "9. k * tot if INT_MIN <= k * tot <= INT_MAX else INT_MAX",
    "10. n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX",
    "1. 42 * tot if INT_MIN <= 42 * tot <= INT_MAX else INT_MAX\n\n2. 0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX\n\n3. -1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX\n\n4. 3.14 * tot if INT_MIN <= 3.14 * tot <= INT_MAX else INT_MAX\n\n5. (2 + 3) * tot if INT_MIN <= (2 + 3) * tot <= INT_MAX else INT_MAX\n\n6. -10 * tot if INT_MIN <= -10 * tot <= INT_MAX else INT_MAX\n\n7. 99 * tot if INT_MIN <= 99 * tot <= INT_MAX else INT_MAX\n\n8. (x + y) * tot if INT_MIN <= (x + y) * tot <= INT_MAX else INT_MAX\n\n9. 7.5 * tot if INT_MIN <= 7.5 * tot <= INT_MAX else INT_MAX\n\n10. (2 * x - y) * tot if INT_MIN <= (2 * x - y) * tot <= INT_MAX else INT_MAX",
    "1. -1",
    "2. 0",
    "3. 1",
    "4. 2",
    "5. 3",
    "6. 10",
    "7. 100",
    "8. 1000",
    "9. -100",
    "10. -1000",
    "-1 * tot if INT_MIN <= -1 * tot <= INT_MAX else INT_MAX",
    "0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX",
    "4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX",
    "5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "6 * tot if INT_MIN <= 6 * tot <= INT_MAX else INT_MAX",
    "7 * tot if INT_MIN <= 7 * tot <= INT_MAX else INT_MAX",
    "8 * tot if INT_MIN <= 8 * tot <= INT_MAX else INT_MAX",
    "x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX",
    "y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX",
    "z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX",
    "a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX",
    "b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX",
    "c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX",
    "d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX",
    "e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX",
    "f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX",
    "g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX",
    "0 * tot if INT_MIN <= 0 * tot <= INT_MAX else INT_MAX",
    "1 * tot if INT_MIN <= 1 * tot <= INT_MAX else INT_MAX",
    "2 * tot if INT_MIN <= 2 * tot <= INT_MAX else INT_MAX",
    "3 * tot if INT_MIN <= 3 * tot <= INT_MAX else INT_MAX",
    "4 * tot if INT_MIN <= 4 * tot <= INT_MAX else INT_MAX",
    "5 * tot if INT_MIN <= 5 * tot <= INT_MAX else INT_MAX",
    "6 * tot if INT_MIN <= 6 * tot <= INT_MAX else INT_MAX",
    "7 * tot if INT_MIN <= 7 * tot <= INT_MAX else INT_MAX",
    "8 * tot if INT_MIN <= 8 * tot <= INT_MAX else INT_MAX",
    "9 * tot if INT_MIN <= 9 * tot <= INT_MAX else INT_MAX",
    "1. x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX\n2. num * tot if INT_MIN <= num * tot <= INT_MAX else INT_MAX\n3. n * tot if INT_MIN <= n * tot <= INT_MAX else INT_MAX\n4. val * tot if INT_MIN <= val * tot <= INT_MAX else INT_MAX\n5. value * tot if INT_MIN <= value * tot <= INT_MAX else INT_MAX\n6. result * tot if INT_MIN <= result * tot <= INT_MAX else INT_MAX\n7. prod * tot if INT_MIN <= prod * tot <= INT_MAX else INT_MAX\n8. multiplier * tot if INT_MIN <= multiplier * tot <= INT_MAX else INT_MAX\n9. number * tot if INT_MIN <= number * tot <= INT_MAX else INT_MAX\n10. factor * tot if INT_MIN <= factor * tot <= INT_MAX else INT_MAX",
    "1. (x * tot if INT_MIN <= x * tot <= INT_MAX else INT_MAX)\n2. (y * tot if INT_MIN <= y * tot <= INT_MAX else INT_MAX)\n3. (z * tot if INT_MIN <= z * tot <= INT_MAX else INT_MAX)\n4. (a * tot if INT_MIN <= a * tot <= INT_MAX else INT_MAX)\n5. (b * tot if INT_MIN <= b * tot <= INT_MAX else INT_MAX)\n6. (c * tot if INT_MIN <= c * tot <= INT_MAX else INT_MAX)\n7. (d * tot if INT_MIN <= d * tot <= INT_MAX else INT_MAX)\n8. (e * tot if INT_MIN <= e * tot <= INT_MAX else INT_MAX)\n9. (f * tot if INT_MIN <= f * tot <= INT_MAX else INT_MAX)\n10. (g * tot if INT_MIN <= g * tot <= INT_MAX else INT_MAX)",
    "1. (x * tot) if INT_MIN <= (x * tot) <= INT_MAX else INT_MAX",
    "2. (y * tot) if INT_MIN <= (y * tot) <= INT_MAX else INT_MAX",
    "3. (z * tot) if INT_MIN <= (z * tot) <= INT_MAX else INT_MAX",
    "4. (a * tot) if INT_MIN <= (a * tot) <= INT_MAX else INT_MAX",
    "5. (b * tot) if INT_MIN <= (b * tot) <= INT_MAX else INT_MAX",
    "6. (c * tot) if INT_MIN <= (c * tot) <= INT_MAX else INT_MAX",
    "7. (num * tot) if INT_MIN <= (num * tot) <= INT_MAX else INT_MAX",
    "8. (value * tot) if INT_MIN <= (value * tot) <= INT_MAX else INT_MAX",
    "9. (result * tot) if INT_MIN <= (result * tot) <= INT_MAX else INT_MAX",
    "10. (prod * tot) if INT_MIN <= (prod * tot) <= INT_MAX else INT_MAX"
  ],
  "num_code_blocks": 183
}
