{
  "template": "[[True] * n for __ in range(n)]",
  "lang": "py",
  "temperature": 0.7,
  "code_blocks": [
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for e in range(n)]",
    "[[True] * n for f in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for item in range(n)]",
    "[[True] * n for element in range(n)]",
    "[[True] * n for idx in range(n)]",
    "[[True] * n for count in range(n)]",
    "[[True] * n for index in range(n)]",
    "[[True] * n for num in range(n)]",
    "[[True] * n for var in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for e in range(n)]",
    "[[True] * n for f in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for e in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for foo in range(n)]",
    "[[True] * n for i in range(10)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for __ in range(5)]",
    "[[True] * n for i in range(100)]",
    "[[True] * n for count in range(n)]",
    "[[True] * n for i in range(len(some_list))]",
    "[[True] * n for i in range(m)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for foo in range(n)]",
    "[[True] * n for bar in range(n)]",
    "[[True] * n for baz in range(n)]",
    "[[True] * n for qux in range(n)]",
    "[[True] * n for spam in range(n)]",
    "[[True] * n for eggs in range(n)]",
    "[[True] * n for i_am_hole in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for item in range(n)]",
    "[[True] * n for idx in range(n)]",
    "[[True] * n for element in range(n)]",
    "[[True] * n for value in range(n)]",
    "[[True] * n for index in range(n)]",
    "[[True] * n for num in range(n)]",
    "[[True] * n for counter in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for idx in range(n)]",
    "[[True] * n for item in range(n)]",
    "[[True] * n for elem in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for item in range(n)]",
    "[[True] * n for element in range(n)]",
    "[[True] * n for index in range(n)]",
    "[[True] * n for num in range(n)]",
    "[[True] * n for count in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for item in range(n)]",
    "[[True] * n for elem in range(n)]",
    "[[True] * n for index in range(n)]",
    "[[True] * n for counter in range(n)]",
    "[[True] * n for loop_var in range(n)]",
    "[[True] * n for itr in range(n)]",
    "[[True] * n for num in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for index in range(n)]",
    "[[True] * n for count in range(n)]",
    "[[True] * n for item in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for elem in range(n)]",
    "[[True] * n for idx in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for m in range(n)]",
    "[[True] * n for value in range(n)]",
    "[[True] * n for count in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for d in range(n)]",
    "[[True] * n for e in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for foo in range(n)]",
    "[[True] * n for bar in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for index in range(n)]",
    "[[True] * n for _ in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for j in range(n)]",
    "[[True] * n for k in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for y in range(n)]",
    "[[True] * n for z in range(n)]",
    "[[True] * n for a in range(n)]",
    "[[True] * n for b in range(n)]",
    "[[True] * n for c in range(n)]",
    "[[True] * n for i in range(n)]",
    "[[True] * n for x in range(n)]",
    "[[True] * n for item in range(n)]",
    "[[True] * n for value in range(n)]",
    "[[True] * n for idx in range(n)]",
    "[[True] * n for element in range(n)]",
    "[[True] * n for num in range(n)]",
    "[[True] * n for i_ in range(n)]",
    "[[True] * n for x_ in range(n)]",
    "[[True] * n for item_ in range(n)]"
  ],
  "num_code_blocks": 300
}
